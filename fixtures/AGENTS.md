# Fixtures

Sample groups and canned whispers, owned jointly by stream B (test fixture
groups for the brain) and stream C (canned whispers for the fake-server client
dev loop). Shapes must match `contracts/schema.py` and `contracts/README.md`.

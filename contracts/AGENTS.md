# Contracts — frozen

These files define the shapes all three streams agreed on. See `README.md` in
this folder for what's here. **Do not edit `schema.py` or `README.md` unless
all three stream owners (A: Core, B: Brain, C: Voice/client) agree** — every
other stream imports or relies on these as-is.
